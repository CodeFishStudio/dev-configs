import type { ConfigType } from '../../types/index.js';
/**
 * Function to add gitignore patterns for a specific config type
 */
export declare const addGitignores: (configType: ConfigType) => Promise<void>;
