import type { ProjectType } from '../../types/index.js';
/**
 * Returns the shared CodeFish `.prettierrc` contents for the given project type.
 */
export declare const prettierRcFileTemplate: (projectType: ProjectType) => string;
